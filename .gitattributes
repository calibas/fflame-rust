# Canonical generated-WGSL dumps are compared against freshly generated
# text by the `canonical_shader_dumps` test. Pin them to LF so a checkout
# on a core.autocrlf machine can't rewrite them into a spurious diff.
tests/shader_dumps/*.wgsl text eol=lf
